//diffrence substraction program
#include <stdio.h>
int main() 
{
    int num1, num2, difference;
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);
    difference = num1 - num2;
    printf("The difference is: %d\n", difference);
    return 0;
}

